namespace Projekt122;
public class WallisschesProdukt
{
    public static double WallisschesProduktBerechnen(int anzahl)
    {
        // Die Berechnung erfolgt nach der "Kurzschreibweise".
        // Jeder Schritt entspricht einer Klammer z.B. (2/1 * 2/3)
        _ = anzahl;
        return 0;
    }
}
